"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import { AgGridReact } from "ag-grid-react";
import type { CellValueChangedEvent, ColDef, GridApi } from "ag-grid-community";
import { AllCommunityModule, ModuleRegistry, themeQuartz } from "ag-grid-community";
import { Download, MessageCircle, Plus, Printer, Search, ShieldCheck, Upload, X } from "lucide-react";
import { CrmInventoryField, CrmInventoryItem, createCrmInventory, createCrmInventoryField, getCrmInventory, getCrmInventoryFields, importCrmInventory, updateCrmInventory } from "@/lib/api";

ModuleRegistry.registerModules([AllCommunityModule]);
export const dynamic = "force-dynamic";
const baseColumns: Array<[string, string]> = [["building_name", "Building"], ["location", "Location"], ["transaction_type", "Transaction"], ["asset_type", "Asset"], ["bhk", "BHK"], ["tower", "Tower"], ["floor", "Floor"], ["area_sqft", "Area sq ft"], ["quote", "Quote"], ["furnishing", "Furnishing"], ["availability", "Availability"], ["contact_name", "Contact"], ["contact_number", "Number"], ["notes", "Notes"]];
const valueOf = (row: CrmInventoryItem, key: string) => key in row ? row[key as keyof CrmInventoryItem] : row.custom_fields?.[key];

export default function PrivateCrmPage() {
  const [rows, setRows] = useState<CrmInventoryItem[]>([]); const [fields, setFields] = useState<CrmInventoryField[]>([]); const [query, setQuery] = useState(""); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState(false); const [newField, setNewField] = useState(""); const [newFieldType, setNewFieldType] = useState<CrmInventoryField["field_type"]>("text"); const [newFieldOptions, setNewFieldOptions] = useState(""); const [selected, setSelected] = useState<CrmInventoryItem[]>([]); const [shareRows, setShareRows] = useState<CrmInventoryItem[]>([]); const [shareKeys, setShareKeys] = useState<string[]>([]); const [mobileEdit, setMobileEdit] = useState<CrmInventoryItem | null>(null); const [company, setCompany] = useState(""); const [senderName, setSenderName] = useState(""); const [senderAddress, setSenderAddress] = useState(""); const [message, setMessage] = useState(""); const [error, setError] = useState(""); const fileRef = useRef<HTMLInputElement>(null); const apiRef = useRef<GridApi<CrmInventoryItem> | null>(null);
  const columns = useMemo(() => [...baseColumns, ...fields.map(field => [field.field_key, field.label] as [string, string])], [fields]);
  const defs = useMemo<ColDef<CrmInventoryItem>[]>(() => columns.map(([key, headerName]) => { const custom = fields.find(field => field.field_key === key); return { field: key as keyof CrmInventoryItem, headerName, editable: true, minWidth: 130, flex: key === "building_name" || key === "location" ? 1.4 : 1, filter: true, sortable: true, resizable: true, valueGetter: params => valueOf(params.data!, key), cellEditor: custom?.field_type === "select" ? "agSelectCellEditor" : custom?.field_type === "checkbox" ? "agCheckboxCellEditor" : key === "area_sqft" || custom?.field_type === "number" || custom?.field_type === "currency" ? "agNumberCellEditor" : undefined, cellEditorParams: custom?.field_type === "select" ? { values: custom.options || [] } : undefined }; }), [columns, fields]);
  async function load() { setLoading(true); try { setRows(await getCrmInventory(query)); setError(""); } catch { setError("Private inventory could not be loaded."); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, [query]); useEffect(() => { void getCrmInventoryFields().then(setFields).catch(() => setError("Custom fields could not be loaded. Apply the CRM migration first.")); }, []);
  async function addRow() { setBusy(true); try { const row = await createCrmInventory({ building_name: "New property", location: "", custom_fields: {} }); setRows(current => [row, ...current]); setMobileEdit(row); setMessage("New listing added."); } catch { setError("A new private row could not be created."); } finally { setBusy(false); } }
  async function addField(event: React.FormEvent) { event.preventDefault(); if (!newField.trim()) return; setBusy(true); try { const field = await createCrmInventoryField({ label: newField, field_type: newFieldType, options: newFieldOptions.split(",").map(item => item.trim()).filter(Boolean) }); setFields(current => [...current.filter(item => item.field_key !== field.field_key), field]); setNewField(""); setNewFieldOptions(""); setMessage(`${field.label} added as a new column.`); } catch { setError("That field could not be added. Check the CRM migration or use another name."); } finally { setBusy(false); } }
  async function onImport(file?: File) { if (!file) return; setBusy(true); try { const result = await importCrmInventory(file); setMessage(`${result.imported} records imported.`); await Promise.all([load(), getCrmInventoryFields().then(setFields)]); } catch { setError("That file could not be imported. Use CSV, TSV, JSON, or Excel with a recognizable property header."); } finally { setBusy(false); if (fileRef.current) fileRef.current.value = ""; } }
  async function changed(event: CellValueChangedEvent<CrmInventoryItem>) { if (!event.data || event.newValue === event.oldValue) return; const key = String(event.colDef.field); const payload: Partial<CrmInventoryItem> = key in event.data ? { [key]: event.newValue } : { custom_fields: { ...(event.data.custom_fields || {}), [key]: event.newValue } }; try { const saved = await updateCrmInventory(event.data.id, payload); setRows(current => current.map(row => row.id === saved.id ? saved : row)); } catch { setError("This cell could not be saved."); void load(); } }
  async function saveMobile() { if (!mobileEdit) return; setBusy(true); try { const saved = await updateCrmInventory(mobileEdit.id, mobileEdit); setRows(current => current.map(row => row.id === saved.id ? saved : row)); setMobileEdit(null); setMessage("Listing updated."); } catch { setError("The listing could not be saved."); } finally { setBusy(false); } }
  function editMobile(row: CrmInventoryItem, key: string, value: string) { setMobileEdit(current => current ? (key in current ? { ...current, [key]: key === "area_sqft" ? Number(value) || null : value } : { ...current, custom_fields: { ...(current.custom_fields || {}), [key]: value } }) : row); }
  function openShare(items: CrmInventoryItem[]) { setShareRows(items); setShareKeys(columns.map(([key]) => key)); }
  function share() { if (!shareRows.length) return; const labels = Object.fromEntries(columns); const text = shareRows.map(row => shareKeys.map(key => valueOf(row, key) ? `${labels[key] || key}: ${valueOf(row, key)}` : "").filter(Boolean).join("\n")).join("\n\n"); window.open(`https://wa.me/?text=${encodeURIComponent(`Hi, sharing ${shareRows.length} ${shareRows.length === 1 ? "property" : "properties"} with you:\n${text}`)}`, "_blank", "noopener,noreferrer"); setShareRows([]); }
  function exportCsv() { apiRef.current?.exportDataAsCsv({ fileName: "private-crm-listings.csv", onlySelected: selected.length > 0 }); }
  function printSheet() { const chosen = selected.length ? selected : rows; const popup = window.open("", "_blank"); if (!popup) return; popup.document.write(`<html><head><title>Property listings</title><style>body{font-family:Arial;padding:24px;color:#17231f}table{border-collapse:collapse;width:100%;font-size:10px}th,td{border:1px solid #9aa79f;padding:6px;text-align:left}th{background:#e8eee9}</style></head><body><h1>${company || "Property listings"}</h1><p>${senderName}${senderAddress ? ` · ${senderAddress}` : ""}</p><table><thead><tr><th>${columns.map(([, label]) => label).join("</th><th>")}</th></tr></thead><tbody>${chosen.map(row => `<tr><td>${columns.map(([key]) => String(valueOf(row, key) ?? "")).join("</td><td>")}</td></tr>`).join("")}</tbody></table><script>window.print()</script></body></html>`); popup.document.close(); }
  return <main className="min-h-screen bg-[var(--bg-base)] text-[var(--text-primary)]"><div className="mx-auto max-w-[1900px] px-4 py-7 sm:px-5 sm:py-10 lg:px-8"><header className="flex flex-col gap-5 border-b border-[var(--border-subtle)] pb-6 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-3xl font-semibold tracking-tight">Private CRM</h1><p className="mt-2 text-sm text-[var(--text-secondary)]">A flexible collection for your brokerage workflow.</p></div><div className="flex flex-wrap gap-2"><button onClick={() => void addRow()} disabled={busy} className="inline-flex items-center gap-2 rounded-lg border border-[var(--border-strong)] px-4 py-2.5 text-sm font-semibold"><Plus size={16} />Add row</button><a href="/chat" className="inline-flex items-center gap-2 rounded-lg bg-[var(--signal-dim)] px-4 py-2.5 text-sm font-semibold text-[var(--parchment)]"><MessageCircle size={16} />Add via chat</a><input ref={fileRef} type="file" accept=".csv,.tsv,.json,.xlsx,.xls,text/csv,application/json,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/vnd.ms-excel" className="hidden" onChange={event => void onImport(event.target.files?.[0])} /><button onClick={() => fileRef.current?.click()} className="inline-flex items-center gap-2 rounded-lg border border-[var(--border-strong)] px-4 py-2.5 text-sm font-semibold"><Upload size={16} />Import file</button></div></header><section className="mt-5 flex items-start gap-3 rounded-xl border border-[var(--signal-dim)]/35 bg-[var(--surface-raised)] p-4 text-sm"><ShieldCheck size={18} className="shrink-0 text-[var(--signal-dim)]" /><p><strong>Private by default.</strong> Every broker can define their own fields.</p></section><div className="mt-5 flex flex-wrap items-center justify-between gap-3"><label className="relative block w-full max-w-md"><Search size={16} className="absolute left-3 top-3 text-[var(--text-secondary)]" /><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search inventory…" className="w-full rounded-lg border border-[var(--border-subtle)] bg-[var(--surface-raised)] py-2.5 pl-9 pr-3 text-sm" /></label><div className="flex items-center gap-2"><span className="text-xs text-[var(--text-secondary)]">{rows.length} records</span><button onClick={exportCsv} className="inline-flex items-center gap-1.5 rounded-md border border-[var(--border-strong)] px-2.5 py-1.5 text-xs font-semibold"><Download size={14} />CSV</button><button onClick={printSheet} className="inline-flex items-center gap-1.5 rounded-md border border-[var(--border-strong)] px-2.5 py-1.5 text-xs font-semibold"><Printer size={14} />PDF / Print</button></div></div>{message && <p className="mt-4 rounded-lg border border-[var(--signal)]/30 bg-[var(--signal)]/10 px-4 py-3 text-sm">{message}</p>}{error && <p role="alert" className="mt-4 rounded-lg border border-[var(--amber)]/40 bg-[var(--amber)]/10 px-4 py-3 text-sm">{error}</p>}<section className="mt-6 overflow-hidden rounded-xl border border-[var(--border-strong)] bg-[var(--surface-raised)]"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-[var(--border-strong)] p-4"><div><h2 className="font-semibold">Inventory collection</h2><p className="mt-0.5 text-xs text-[var(--text-secondary)]">Mobile cards below. Desktop grid supports editing, filtering, sorting, and reordering.</p></div><form onSubmit={addField} className="flex flex-wrap gap-2"><input value={newField} onChange={event => setNewField(event.target.value)} placeholder="New field…" aria-label="New custom field" className="w-32 rounded-md border border-[var(--border-strong)] bg-[var(--bg-base)] px-2.5 py-1.5 text-xs" /><select value={newFieldType} onChange={event => setNewFieldType(event.target.value as CrmInventoryField["field_type"])} className="rounded-md border border-[var(--border-strong)] bg-[var(--bg-base)] px-2 py-1.5 text-xs"><option value="text">Text</option><option value="number">Number</option><option value="date">Date</option><option value="currency">Currency</option><option value="checkbox">Checkbox</option><option value="select">Select</option></select>{newFieldType === "select" && <input value={newFieldOptions} onChange={event => setNewFieldOptions(event.target.value)} placeholder="A, B" className="w-24 rounded-md border border-[var(--border-strong)] bg-[var(--bg-base)] px-2 py-1.5 text-xs" />}<button disabled={busy} className="inline-flex items-center gap-1 rounded-md border border-[var(--border-strong)] px-2.5 py-1.5 text-xs font-semibold"><Plus size={13} />Add field</button></form></div>{selected.length > 0 && <div className="flex flex-wrap items-center justify-between gap-2 border-b border-[var(--border-strong)] bg-[var(--signal-dim)]/5 px-4 py-2.5 text-sm"><span>{selected.length} selected</span><button onClick={() => openShare(selected)} className="rounded-md bg-[var(--signal-dim)] px-3 py-1.5 text-xs font-semibold text-white">Choose fields & share</button></div>}<div className="lg:hidden p-3 pb-20">{rows.map(row => { const editing = mobileEdit?.id === row.id; const visible = ["location", "bhk", "floor", "area_sqft", "quote", "furnishing", "availability"]; return <article key={row.id} className="mb-3 rounded-xl border border-[var(--border-strong)] bg-[var(--bg-base)] p-3"><div className="flex items-start gap-3"><input type="checkbox" checked={selected.some(item => item.id === row.id)} onChange={event => setSelected(current => event.target.checked ? [...current, row] : current.filter(item => item.id !== row.id))} className="mt-1 h-4 w-4" aria-label={`Select ${row.building_name || "property"}`} /><div className="min-w-0 flex-1"><h3 className="truncate font-semibold">{row.building_name || "New property"}</h3><p className="truncate text-xs text-[var(--text-secondary)]">{String(valueOf(row, "location") || "Location not added")}</p></div><button onClick={() => editing ? void saveMobile() : setMobileEdit({ ...row, custom_fields: { ...(row.custom_fields || {}) } })} className="rounded-md border border-[var(--border-strong)] px-2.5 py-1.5 text-xs font-semibold">{editing ? "Save" : "Edit"}</button><button onClick={() => openShare([row])} className="rounded-md bg-[var(--signal-dim)] px-2.5 py-1.5 text-xs font-semibold text-white">Share</button></div><div className="mt-3 grid grid-cols-2 gap-2">{visible.map(key => { const label = columns.find(([columnKey]) => columnKey === key)?.[1] || key; const value = editing ? valueOf(mobileEdit!, key) : valueOf(row, key); return <label key={key} className="rounded-lg border border-[var(--border-subtle)] p-2 text-xs"><span className="block text-[10px] uppercase tracking-wide text-[var(--text-secondary)]">{label}</span>{editing ? <input value={String(value ?? "")} onChange={event => editMobile(row, key, event.target.value)} className="mt-1 w-full bg-transparent font-medium outline-none" /> : <span className="mt-1 block font-medium">{String(value ?? "") || "—"}</span>}</label>; })}</div></article>; })}</div><div className="hidden lg:block"><div className="ag-theme-quartz h-[620px] w-full"><AgGridReact<CrmInventoryItem> theme={themeQuartz} rowData={loading ? [] : rows} columnDefs={defs} defaultColDef={{ editable: true, resizable: true, sortable: true, filter: true, floatingFilter: true }} rowSelection={{ mode: "multiRow", checkboxes: true, headerCheckbox: true }} onGridReady={event => { apiRef.current = event.api; }} onSelectionChanged={event => setSelected(event.api.getSelectedRows())} onCellValueChanged={event => void changed(event)} getRowId={params => String(params.data.id)} /></div></div></section><section className="mt-5 rounded-xl border border-[var(--border-strong)] bg-[var(--surface-raised)] p-4"><h2 className="font-semibold">Client sheet details</h2><p className="mt-1 text-xs text-[var(--text-secondary)]">These details appear on PDF / Print exports.</p><div className="mt-3 grid gap-2 md:grid-cols-3"><input value={company} onChange={event => setCompany(event.target.value)} placeholder="Company name" className="rounded-md border border-[var(--border-strong)] bg-[var(--bg-base)] px-3 py-2 text-sm" /><input value={senderName} onChange={event => setSenderName(event.target.value)} placeholder="Sender name" className="rounded-md border border-[var(--border-strong)] bg-[var(--bg-base)] px-3 py-2 text-sm" /><input value={senderAddress} onChange={event => setSenderAddress(event.target.value)} placeholder="Office address / contact" className="rounded-md border border-[var(--border-strong)] bg-[var(--bg-base)] px-3 py-2 text-sm" /></div></section>{shareRows.length > 0 && <div role="dialog" aria-modal="true" className="fixed inset-0 z-50 flex items-center justify-center bg-black/30 p-4"><div className="w-full max-w-lg rounded-xl bg-[var(--surface-raised)] p-5 shadow-xl"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold">Choose fields for {shareRows.length} {shareRows.length === 1 ? "property" : "properties"}</h2><button onClick={() => setShareRows([])} aria-label="Close"><X size={18} /></button></div><p className="mt-1 text-sm text-[var(--text-secondary)]">The selected listings will be combined into one WhatsApp message.</p><div className="mt-3 flex gap-3 text-xs"><button onClick={() => setShareKeys(columns.map(([key]) => key))} className="text-[var(--signal-dim)]">Select all</button><button onClick={() => setShareKeys([])} className="text-[var(--text-secondary)]">Clear all</button><span>{shareKeys.length} selected</span></div><div className="mt-3 grid max-h-72 grid-cols-2 gap-2 overflow-y-auto">{columns.map(([key, label]) => <label key={key} className="flex cursor-pointer items-center gap-2 rounded-md px-2 py-1.5 text-sm"><input type="checkbox" checked={shareKeys.includes(key)} onChange={event => setShareKeys(current => event.target.checked ? [...new Set([...current, key])] : current.filter(item => item !== key))} />{label}</label>)}</div><div className="mt-5 flex justify-end gap-2"><button onClick={() => setShareRows([])} className="rounded-lg border border-[var(--border-strong)] px-3 py-2 text-sm">Cancel</button><button onClick={share} disabled={!shareKeys.length} className="rounded-lg bg-[var(--signal-dim)] px-3 py-2 text-sm font-semibold text-white">Open WhatsApp</button></div></div></div>}</div></main>;
}
